@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ContentModels.LeaderboardPage>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;
@{
    Layout = "master.cshtml";

    // Fetch all records for the global leaderboard
    var allRuns = Model.Children()
        .Where(x => x.ContentType.Alias == "runRecord")
        .OrderByDescending(x => x.Value<int>("encountersSurvived"))
        .ThenByDescending(x => x.Value<int>("enemiesKilled"));
}

<style>
    /* page-level layout styles*/
    h1 { color: #ff5555; text-transform: uppercase; letter-spacing: 2px; text-align: center; }
    .description { margin-bottom: 2rem; color: #8b949e; text-align: center; }
</style>

<h1>@Model.Value("title")</h1>
<div class="description">
    @Html.Raw(Model.Value("description"))
</div>

<!-- Call our leaderboard -->
@await Html.PartialAsync("Partials/_LeaderboardTable", allRuns)